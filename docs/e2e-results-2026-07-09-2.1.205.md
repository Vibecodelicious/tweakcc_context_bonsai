# Context Bonsai E2E Results: Claude Code 2.1.205 Linux x64

- Date (UTC): 2026-07-09
- Target: `@anthropic-ai/claude-code@2.1.205` / platform `@anthropic-ai/claude-code-linux-x64@2.1.205`
- Runtime under test: native binary by explicit path `/tmp/cc-bonsai-cycle-2.1.205/claude-2.1.205` (pristine sha256 `dd8734c0b6a503fe1d17425184e57b397c30bb0337a33f1470d9985febfe5b09`; `--version` = `2.1.205 (Claude Code)`). The live installed CLI (2.1.198) was never read or modified.
- Patch state under test: `bun run apply --path <binary>` applied `archived-filter`, `message-content-ids`, `context-bonsai-gauge` with all three sentinels verified; `<binary> mcp list` shows `context-bonsai: ... ✔ Connected`.
- Evidence JSON (local-only, uncommitted): under `/tmp/cc-bonsai-e2e/`.

## Scenario verdicts

| Scenario | Verdict | Reason code | Evidence |
|---|---|---|---|
| E2E-00 install procedure | PASS | install-procedure-pass | apply exits 0 with 3 sentinels; `mcp list` connected; end-of-gate `apply --restore` returns binary to pristine sha256 `dd8734c0...`; all commands from README (with the two recorded adaptations: explicit `--path`, MCP block). |
| Pinned-target artifact evidence | PASS | — | `bun run e2e/native-e2e.ts artifact-evidence` exit 0; composition + all 3 patch sentinels verified against bundle sha256 `4b58a8db...`; validates `docs/semantic-anchor-analysis-2.1.205.md`. `/tmp/cc-bonsai-e2e/2.1.205-artifact-evidence.json`. |
| E2E-07 / Protocol A secret-prune oracle | PASS | — | `protocol-a-oracle --secret vermilliontractor`: `valid:true`, `occurrenceCount:1`, `invalidOccurrenceCount:0`, verdict "secret appears only in archived original blocks". The secret appears in exactly one row (`type:user`, `archived:true`); it never enters the summary or any non-archived row. `/tmp/cc-bonsai-e2e/2.1.205-protocol-a-oracle.json`. |
| E2E-08 / prune transform effect | PASS | — | `prune-effect`: verdict PASS. `archivedRangePresentPre:true`, `archivedRangeVisiblePost:false`, `placeholderPresentPost:true`, `rangeVisibleCharsPre:323`, `rangeVisibleCharsPost:0`, `footprintDropped:true`, 6 range uuids archived. Archived content is genuinely ABSENT from the post-prune model-visible payload and the range's visible footprint drops to 0 (only the small placeholder summary remains). Transform-EFFECT oracle, not flag/recall. `/tmp/cc-bonsai-e2e/2.1.205-prune-effect.json`, `/tmp/cc-bonsai-e2e/2.1.205-e2e08-{pre,post}.jsonl`. |
| E2E-04 gauge cadence/severity | PARTIAL | gauge-live-render-not-driven | Live gauge cadence/severity rendering was not driven (no bound live gauge driver exists at source HEAD) — the standing conditional exception. Both compensating conditions hold: (a) the gauge anchors select and the gauge patch composes with sentinel verification against the frozen bundle (artifact-evidence PASS); (b) a live `-p "Reply with exactly: PONG" --output-format json` run surfaced the accumulator usage record in `modelUsage` — `jq -e '.modelUsage \| to_entries \| any(.value \| has("inputTokens") and has("contextWindow") and has("maxOutputTokens"))'` exits 0, behaviorally confirming the token-usage seam. Additionally, all gauge unit tests (`patches/context-bonsai-gauge.patch.test.ts`) PASS. `/tmp/cc-bonsai-e2e/2.1.205-e2e04-modelusage.json`. |

## Live-drive notes

- The seeded prune session used unique boundary codewords (alpha / omega) and an in-range secret token; each matched exactly one string-uuid row in the pre-prune session. The model acknowledged each seed with a bare "ok" and did not echo the tokens (§3.6 seeding discipline).
- The prune was driven by instructing the model to recall the two codewords from memory and place them only inside the `context-bonsai-prune` tool call (never in the plain user prompt) — a plain-prompt repetition of a boundary phrase creates a second searchable match and correctly triggers the ambiguity fail-closed path ("from_pattern matched multiple messages"), which was observed and recovered by re-driving with the recall-based instruction. Tool-call argument messages are excluded from the candidate set by the prune-wrapper filter (Behavioral Constraint 7), so recalled codewords in the tool call do not inflate the match count.
- Harness argument note: with `--allowedTools` present, the native binary did not parse a trailing positional prompt (it waited on stdin); the prompt was supplied via stdin instead. Recorded as an executor mechanics note, not a product defect.

## Behavioral constraints

All named regression suites pass under `bun test` (165 pass, 0 fail): guard identification, `isError` fail-closed surfaces, prune-wrapper collision filter, searchable-text reach, marker coverage incl. system rows, retrieve marker removal, provider-map `api_system` filtering, and the two Constraint-10 compatibility tests (E2E-05 `compat-error-unit-verified`).
