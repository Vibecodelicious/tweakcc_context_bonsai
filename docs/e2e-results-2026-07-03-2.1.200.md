# Context Bonsai E2E Results — Claude Code native 2.1.200 Linux x64

- Run date (UTC): 2026-07-03
- Target: `@anthropic-ai/claude-code@2.1.200`; runtime under test = frozen native binary `/tmp/cc-bonsai-artifacts/claude-code/2.1.200/native/claude-2.1.200` (binary sha256 `26e42a3268979f0c5a3b6c0f375b15dd7decfaae4bb02774390d6a23f4cd51ad`, reports `2.1.200 (Claude Code)`).
- Source: `tweakcc_context_bonsai` `main`, frozen `SOURCE_HEAD_SHA=f92dfac9c5daecc286e03b90ef20bb930cf68818`.
- Live-install protection: the live installed CLI (`claude --version` = `2.1.198`) and `~/.local/share/claude/versions/` (`2.1.185`, `2.1.198`) were never modified; every scenario launched the frozen binary by explicit path. The patched binary was `--restore`d to its pristine sha256 at end of gate.
- Extracted bundle: sha256 `60e1c6cfc6d3931bf44020bfdb397d925a786365b8743a9760f2098aca9d7597`, `18698041` bytes.
- Executor: Opus-4.8-low subagent, REAL-CYCLE. Standing boundary: no outward action (no push/publish) on this run.

## Reason Codes

- `install-procedure-pass` — E2E-00 local README-procedure verdict predicate met (Amendment 1).
- `prune-success` — contiguous prune archived the range; placeholder summary written.
- `ambiguity-rejected` — deterministic plain-text ambiguity error; no archive-state mutation.
- `retrieve-restore` — archived range restored; placeholder removed.
- `gauge-live-render-not-driven` — E2E-04 gauge cadence/severity not driven; `PARTIAL` covered by the recorded conditional exception (Amendment 1) with both compensating conditions evidenced.
- `compat-error-unit-verified` — E2E-05 fail-closed compatibility paths verified by two net-new Constraint-10 unit tests (Amendment 1).
- `resume-persistence` — archived state survived `--resume`.
- `secret-oracle-pass` — Protocol A: secret only in archived original blocks, never in prune args/summary/index.
- `prune-guard-live-pass` — E2E-08 direct versioned-path launch, no `--resume`, bug-shape confirmed; live prune drove and content was removed with footprint drop.

## Scenario Verdicts

| Scenario | Verdict | Reason code | Evidence |
|---|---|---|---|
| E2E-00 clean install procedure | PASS | `install-procedure-pass` | `bun run apply --path "$TARGET_NATIVE_BINARY"` exit 0, sentinels for archived-filter + message-content-ids + context-bonsai-gauge verified; `"$TARGET_NATIVE_BINARY" mcp list` → `context-bonsai: … ✔ Connected`; end-of-gate `--restore` returned binary to pristine sha256 `26e42a32…`; only README commands used (with the two recorded adaptations: `--path` explicit binary, MCP block from this checkout). |
| E2E-01 contiguous prune success | PASS | `prune-success` | Live prune drive on a seeded session (throwaway first message per Amendment 2 seeding rule; ALPHA-PHRASE-001 … OMEGA-PHRASE-001). Post-session shows the range marked `archived:true` and a `summary`-typed placeholder entry (`compactMetadata.fromMessageId/toMessageId`, messageCount 18). |
| E2E-02 ambiguity / unresolved rejection | PASS | `ambiguity-rejected` | `from_pattern "SHARED-PHRASE"` (2 string-uuid matches) → deterministic tool error "from_pattern matched multiple messages. Provide a more specific pattern."; post-drive archive state unmutated (0 `summary` rows, 0 `archived` rows, no marker file). The raw session-file hash grows only by the failed prune turn's own appended `tool_use`/`tool_result` rows (normal conversation append), not by any archive mutation. Wrapper-collision filtering (Constraint 7) additionally covered by `mcp-server/index.test.ts` "prune call filtering" tests. |
| E2E-03 retrieve by anchor success | PASS | `retrieve-restore` | `context-bonsai-retrieve` with `anchor_id 1258fa65-…` (via `--resume`) restored the full range; post-retrieve session shows 0 `archived` rows and 0 `summary` placeholders; model recalled ALPHA/OMEGA content verbatim. |
| E2E-04 gauge cadence and severity | PARTIAL (exception-covered) | `gauge-live-render-not-driven` | No bound live gauge-render driver exists. Compensating evidence per Amendment 1: (a) gauge anchors select and the gauge patch composes with sentinel verification against the frozen bundle (Phase 4 `apply` + artifact-evidence exit 0); (b) live `--print --output-format json` run surfaces the re-derived accumulator record in `modelUsage`: `inputTokens`, `contextWindow` (200000 / 1000000), `maxOutputTokens` (32000 / 64000) — behaviorally confirming the re-derived `token-usage` seam `a6p`. Evidence `/tmp/cc-bonsai-e2e/2.1.200-e2e04-modelusage.json`. |
| E2E-05 compatibility error path | PASS (unit-verified) | `compat-error-unit-verified` | Two net-new Constraint-10 tests in `mcp-server/index.test.ts`: (1) missing session JSONL → `isError:true`, body compatibility error, no file created; (2) malformed non-final line → `readSessionMessages` corruption throw → `isError:true`, compatibility error, file bytes unchanged. Both pass in the 165-test suite. |
| E2E-06 persistence across resume | PASS | `resume-persistence` | Pruned session resumed via `"$TARGET_NATIVE_BINARY" --resume <sid>`; archived state persisted (5 `archived` rows before and after resume) and the E2E-03 retrieve itself operated correctly on archived state through a resumed session; secret stayed confined to archived rows post-resume. |
| E2E-07 secret prune oracle (Protocol A) | PASS | `secret-oracle-pass` | `protocol-a-oracle`: `occurrenceCount 1`, `invalidOccurrenceCount 0`, verdict "PASS: secret appears only in archived original blocks". Secret (fresh uncommon word) never appeared in prune arguments, summary, or index terms. Evidence `/tmp/cc-bonsai-e2e/2.1.200-protocol-a-oracle.json`. |
| E2E-08 bug-shape prune guard (direct launch) | PASS | `prune-guard-live-pass` | Launch: `prune-guard-live` verdict PASS — `argv0IsVersionedBinary true`, `hasResumeFlag false`, `bugShapeConfirmed true`, exit 0 (`/tmp/cc-bonsai-e2e/2.1.200-prune-guard-live.json`). Transform effect: `prune-effect` verdict PASS — `rangeVisibleCharsPre 577 → Post 0`, `footprintDropped true`, `placeholderPresentPost true`, `archivedRangeVisiblePost false` (`/tmp/cc-bonsai-e2e/2.1.200-prune-effect.json`). A transform-EFFECT oracle, not flag/recall-based. |
| Pinned-target artifact evidence | PASS | (semantic + mechanical) | `artifact-evidence` exit 0 against the frozen bundle; all 8 anchors select; `docs/semantic-anchor-analysis-2.1.200.md` validates (8 sections, 10 fields each, reclassification phrases). `/tmp/cc-bonsai-e2e/2.1.200-artifact-evidence.json`. |

## Release-gate summary

Full immutable scope satisfied: E2E-00/01/02/03/05/06/07/08 and pinned-target artifact evidence PASS; E2E-04 `PARTIAL` covered by the recorded Amendment-1 conditional exception with both compensating conditions evidenced. No `BLOCKED` scenarios. No omitted scenarios. Live install untouched.

## Evidence (local-only, not committed)

- `/tmp/cc-bonsai-e2e/2.1.200-artifact-evidence.json`
- `/tmp/cc-bonsai-e2e/2.1.200-prune-guard-live.json`
- `/tmp/cc-bonsai-e2e/2.1.200-prune-effect.json`
- `/tmp/cc-bonsai-e2e/2.1.200-protocol-a-oracle.json`
- `/tmp/cc-bonsai-e2e/2.1.200-e2e04-modelusage.json`
- `/tmp/cc-bonsai-e2e/baseline/` (rows 01–12 command logs)

No secrets, credentials, auth paths, session transcripts, or extracted bundle contents appear in this or any committed artifact.
