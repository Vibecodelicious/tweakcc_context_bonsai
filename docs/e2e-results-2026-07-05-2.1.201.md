# Context Bonsai E2E Results — Claude Code native 2.1.201 Linux x64

- Run date (UTC): 2026-07-05
- Target: `@anthropic-ai/claude-code@2.1.201`; runtime under test = frozen native binary `/tmp/cc-bonsai-artifacts/claude-code/2.1.201/native/claude-2.1.201` (binary sha256 `a34809a6839fdefff21b9347d7fb5b6b58e6a9cc208a5e62853f29c83eb107a3`, reports `2.1.201 (Claude Code)`).
- Source: `tweakcc_context_bonsai` `main`, frozen `SOURCE_HEAD_SHA=22a0bd06958cd8d60cf4adfe48aa7b6ed08c962c` (executed at `HEAD=6321e342f6b432aa02452aae9cefcc48e24af320`; relay-drift allowlist + §3.5 byte-identity re-check passed).
- Live-install protection: the live installed CLI (`claude --version` = `2.1.198`) and `~/.local/share/claude/versions/` (`2.1.185`, `2.1.198`) were never modified; every scenario launched the frozen binary by explicit path. The patched binary was `--restore`d to its pristine sha256 `a34809a6…` at end of gate (verified).
- Extracted bundle: sha256 `9f9519b0c93914bd2fda5e6cdc7a74df2b7121909f783fb3b1cc9c86771708ef`, `18698064` bytes.
- Executor: Opus-4.8-low subagent, REAL-CYCLE. Standing boundary: no outward action (no push/publish) on this run; Landing Authorization ABSENT (Phase 8 not run).

## Reason Codes

- `install-procedure-pass` — E2E-00 local README-procedure verdict predicate met (bound Amendment-1 path).
- `prune-success` — contiguous prune archived the range; placeholder summary written.
- `ambiguity-rejected` — deterministic plain-text tool ambiguity error; no archive-state mutation.
- `retrieve-restore` — archived range restored; placeholder removed.
- `gauge-live-render-not-driven` — E2E-04 gauge cadence/severity not driven; `PARTIAL` covered by the recorded conditional exception with both compensating conditions evidenced.
- `compat-error-unit-verified` — E2E-05 fail-closed compatibility paths verified by the two Constraint-10 unit tests present at `SOURCE_HEAD_SHA`.
- `resume-persistence` — archived state survived `--resume`.
- `secret-oracle-pass` — Protocol A: secret only in archived original block, never in prune args/summary/index.
- `prune-guard-live-pass` — E2E-08 direct versioned-path launch, no `--resume`, bug-shape confirmed; live prune drove and content was removed with footprint drop.

## Live-Drive Discipline (§3.6)

Multi-turn sessions were driven non-interactively via `"$TARGET_NATIVE_BINARY" -p <msg> --session-id/--resume <sid>`, sharing one session JSONL. Boundary phrases were fresh, run-unique, seeded not on the first message, each with a bare-"ok"/never-repeat instruction (each matched exactly one string-uuid transcript row). Prune drives referenced the boundary tokens by description (recall), never writing them literally into the drive prompt, to avoid corpus-duplication ambiguity. Run-unique phrases used: `ZEPHYRQUAIL7`/`MARBLEFINCH3` (main session, secret `WOMBATLANTERN9`), `KESTRELVIOLET5`/`BASALTHERON8` (E2E-06), `SHAREDPHRASEQ` (E2E-02 deliberate collision).

## Scenario Verdicts

| Scenario | Verdict | Reason code | Evidence |
|---|---|---|---|
| E2E-00 clean install procedure | PASS | `install-procedure-pass` | `bun run apply --path "$TARGET_NATIVE_BINARY"` exit 0, patches applied: archived-filter + message-content-ids + context-bonsai-gauge (apply verifies sentinels, fails closed otherwise); `"$TARGET_NATIVE_BINARY" mcp list` → `context-bonsai: … ✔ Connected`; end-of-gate `--restore` returned binary to pristine sha256 `a34809a6…`; only README commands used (two recorded adaptations: `--path` explicit binary, MCP block from this checkout). |
| E2E-01 contiguous prune success | PASS | `prune-success` | Live prune on the main seeded session (throwaway first message; ZEPHYRQUAIL7 … MARBLEFINCH3). POST shows a `summary`-typed placeholder `[ARCHIVED RANGE ffd103bf…..29399865…]` with `compactMetadata.fromMessageId/toMessageId`, `messageCount 18`, and 7 `archived:true` rows. Anchor id returned: `ffd103bf-e1ed-4467-818a-97bc39fec4df`. Evidence `/tmp/cc-bonsai-e2e/2.1.201-e2e08-{pre,post}.jsonl`. |
| E2E-02 ambiguity / unresolved rejection | PASS | `ambiguity-rejected` | Forced tool call with `from_pattern="SHAREDPHRASEQ"` (matches multiple string-uuid rows) → deterministic tool error verbatim: `from_pattern matched multiple messages. Provide a more specific pattern.` Pre/post archive state unmutated (0 `archived` rows, 0 `summary` rows both sides). Wrapper-collision filtering (Constraint 7) additionally covered by `mcp-server/index.test.ts` prune-wrapper-filter tests. Evidence `/tmp/cc-bonsai-e2e/2.1.201-e2e02-forced.json`, `2.1.201-e2e02-post.jsonl`. |
| E2E-03 retrieve by anchor success | PASS | `retrieve-restore` | `context-bonsai-retrieve` with `anchor_id ffd103bf-…` (via `--resume`) restored the full range; post-retrieve session shows 0 `archived` rows and 0 `summary` placeholders. Evidence `/tmp/cc-bonsai-e2e/2.1.201-e2e03-postretrieve.jsonl`. |
| E2E-04 gauge cadence and severity | PARTIAL (exception-covered) | `gauge-live-render-not-driven` | No bound live gauge-render driver exists at `SOURCE_HEAD_SHA`. Compensating evidence: (a) gauge anchors (`token-usage`/`attachment-pipeline`/`reminder-render`) select and the gauge patch composes with sentinel verification against the frozen bundle (Phase 4 `apply` exit 0 + artifact-evidence exit 0); (b) live `-p "Reply with exactly: PONG" --output-format json` surfaces the accumulator record in `modelUsage` — `jq -e '.modelUsage | to_entries | any(.value | has("inputTokens") and has("contextWindow") and has("maxOutputTokens"))'` exits 0. Evidence `/tmp/cc-bonsai-e2e/2.1.201-e2e04-modelusage.json`. |
| E2E-05 compatibility error path | PASS (unit-verified) | `compat-error-unit-verified` | The two Constraint-10 tests in `mcp-server/index.test.ts` (E2E-05 describe block, lines 400/426): (1) prune on missing session JSONL → `isError:true`, compatibility error, no file created; (2) malformed non-final line → `isError:true`, file bytes unchanged. Both pass in the 165-test suite (`bun test` rows 02/05, and the 38-test `index.test.ts` run: 38 pass, 0 fail). |
| E2E-06 persistence across resume | PASS | `resume-persistence` | Fresh session (KESTRELVIOLET5 … BASALTHERON8), pruned (anchor `726856d9-…`), then resumed via `"$TARGET_NATIVE_BINARY" --resume <sid>`: `archived:true` rows = 5 before AND after resume. Additionally, the main session's E2E-03 retrieve itself operated on archived state through a resumed session. Evidence `/tmp/cc-bonsai-e2e/2.1.201-e2e06b-postresume.jsonl`. |
| E2E-07 secret prune oracle (Protocol A) | PASS | `secret-oracle-pass` | `protocol-a-oracle` on the main-session POST: `valid true`, `occurrenceCount 1`, `invalidOccurrenceCount 0`, verdict "PASS: secret appears only in archived original blocks". Secret `WOMBATLANTERN9` appears only in the archived string-uuid user row (line 19, `archived:true`); the two uuid-null metadata echoes are correctly excluded. Never in prune arguments, summary, or index terms. Evidence `/tmp/cc-bonsai-e2e/2.1.201-protocol-a-oracle.json`. |
| E2E-08 bug-shape prune guard (direct launch) | PASS | `prune-guard-live-pass` | Launch: `prune-guard-live` verdict PASS — `argv0IsVersionedBinary true`, `hasResumeFlag false`, `bugShapeConfirmed true`, exit 0 (`/tmp/cc-bonsai-e2e/2.1.201-prune-guard-live.json`). Transform effect: `prune-effect` verdict PASS — `rangeVisibleCharsPre 417 → Post 0`, `footprintDropped true`, `placeholderPresentPost true`, `archivedRangeVisiblePost false` (`/tmp/cc-bonsai-e2e/2.1.201-prune-effect.json`). A transform-EFFECT oracle, not flag/recall-based. |
| Pinned-target artifact evidence | PASS | (semantic + mechanical) | `artifact-evidence` exit 0 against the frozen bundle (baseline row 04 + post-replay row 07); all 8 anchors select at the generation-table offsets/scores/counts (zero drift, +23-byte uniform shift); `docs/semantic-anchor-analysis-2.1.201.md` validates (8 sections, 10 fields each, reclassification phrases). Evidence `/tmp/cc-bonsai-e2e/2.1.201-artifact-evidence.json`. |

## Release-gate summary

Full immutable scope satisfied: E2E-00/01/02/03/05/06/07/08 and pinned-target artifact evidence PASS; E2E-04 `PARTIAL` covered by the recorded conditional exception with both compensating conditions evidenced. No `BLOCKED` scenarios. No omitted scenarios. Live install untouched (`claude --version` = `2.1.198` unchanged; versions dir unchanged).

## Evidence (local-only, not committed)

- `/tmp/cc-bonsai-e2e/2.1.201-artifact-evidence.json`
- `/tmp/cc-bonsai-e2e/2.1.201-prune-guard-live.json`
- `/tmp/cc-bonsai-e2e/2.1.201-prune-effect.json`
- `/tmp/cc-bonsai-e2e/2.1.201-protocol-a-oracle.json`
- `/tmp/cc-bonsai-e2e/2.1.201-e2e04-modelusage.json`
- `/tmp/cc-bonsai-e2e/2.1.201-e2e08-{pre,post}.jsonl`, `2.1.201-e2e03-postretrieve.jsonl`, `2.1.201-e2e06b-postresume.jsonl`, `2.1.201-e2e02-{forced.json,post.jsonl}`
- `/tmp/cc-bonsai-e2e/baseline/` (rows 01–07 command logs)

No secrets, credentials, auth paths, session transcripts, or extracted bundle contents appear in this or any committed artifact.
