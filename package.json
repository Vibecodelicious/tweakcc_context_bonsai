{
  "name": "tweakcc-context-bonsai",
  "version": "0.1.1",
  "type": "module",
  "description": "Context Bonsai for Claude Code: context-bonsai MCP server plus tweakcc patch tooling.",
  "bin": {
    "context-bonsai": "./mcp-server/index.ts"
  },
  "scripts": {
    "apply": "bun run apply/apply-bonsai.ts",
    "apply:restore": "bun run apply/apply-bonsai.ts --restore",
    "test": "bun test",
    "typecheck": "bun x tsc --noEmit && cd mcp-server && bun x tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "tweakcc": "^4.0.13"
  }
}
