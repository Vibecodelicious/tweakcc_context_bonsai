# Hand-Off

Entry point for a fresh session in the relay chain. Everything needed to start is on this page; follow pointers only when the work requires them. At the end of your work unit, update this file (re-read `.llm-conductor/writing_guidance/DOCUMENT_WRITING_GUIDANCE.md` first — intent rule 5 below). Authorities, on demand:

- **Direction**: the parent repo's `docs/meta-loop-direction.md` (parent repo = `/home/basil/projects/context-bonsai-agents/`; all parent paths below are relative to it). Its "Owner direction, 2026-07-03" entry is the contract for the current work — read it before changing course.
- **OpenCode pilot** (executor now `openai/gpt-5.3-codex-spark`, owner decision 2026-07-04; Zen/`opencode/*` forbidden): current state lives in the parent's `.agents/pilot/gpt55-v1.17.13-observer-log.md`. Runs 5/5b were false starts (quota mis-route; headless permission hang — harness-fixed at parent `73dc40e`). **Run 5e CLOSED 2026-07-05** at an observer-verified genuine STOP (Phase 4: the replayed commits import v1.15.7 APIs — `@/bus/bus-event` et al. — absent in v1.17.13; reproduced on clean replay state). Track B outcome: the intent-log + reconciliation scaffolding **worked** — truthful reports across 8 process lifetimes, 7 resumes, false-prose self-correction, one honest self-attributed EXECUTOR-FAIL; four-item tuning batch recorded in the observer log for the next brief revision. The Track B tuning batch is **applied** (brief at parent `d24540f`). **Track A unit complete 2026-07-05 under the DRIVE rule**: spec §1.15 gained the target-resolution rehearsal (parent `461dc8b`, adversarially reviewed); the port was **re-implemented against v1.17.13 on the Fable tier** (the cherry-picks had pasted v1.15.7's monolith over upstream's schema/core refactor — real capability: optional message `metadata` in the shared schema, `MessageV2.update` with projector-consistent event publish, registry sanitize + plugin ToolContext bridge) and rehearsed green; **plan revision 2 approved** through the full §1.15 loop (parent `8d8af58`; Phase 3 = `git am` of the checksummed patch artifact; §2.5 re-implement rows with behavioral contracts; judge REJECT→APPROVE after a provenance-citation fix). Run-5e state swept (records in `archive/run5e-v1.17.13/`, baseline reverted `48263a4`). **Run 6 CLOSED CLEAN 2026-07-05 (observer-verified)**: revision 2 executed in 16 minutes, zero stumbles — baseline committed (`5054f94`), patch series applied first-try, all validation and seal gates green, correct fail-closed no-seal on gate 11 (`credentials-missing-in-harness`), truthful §1.16 + final report needing zero correction. Independent verification: 61 tests green on the replay worktree, nothing pushed, spec untouched. **Both track theses landed in one run.** Remaining for a full seal: Basil supplies `OPENCODE_PROVIDER/MODEL/API_KEY` (slacked, `ok`), then re-run the blocked e2e scenarios and the Basil-gated publish ladder. Run-6 state (worktree with the 3 replay commits, local tag `bonsai/v1-on-opencode-1.17.13`, baseline commit) is the live cycle state — preserve it; the next run resumes at the credentials gate, not from scratch.

**Track B final goal reached (2026-07-05, unit advanced Track B)**: the run-continuity scaffolding is generalized into the meta-loop spec as §1.18 (parent `031e533`, adversarially reviewed — two blocking findings fixed, incl. scoping the resume rule so it cannot weaken §1.14's generation-collision guard). **Direction-doc iteration DONE (2026-07-05, unit advanced Track A)**: the parent's `docs/meta-loop-direction.md` is superseded in place at parent `d94922f` — Current State/Next Step/Provisional Future Steps absorb runs 5–6, the sealed CC cycle, the §1.15 rehearsal and §1.18; the End Goal tiering paragraph carries the deferred generalization (per-harness tiers, gpt-5.3-codex-spark, two-layer scaffolding), every element with recorded owner provenance. Reviewed per the writing guidance: source-truth clean, all reader-state findings fixed. **Closed-artifact rehearsal DONE (2026-07-05, unit advanced Track A — direction-doc Provisional Future Step 2)**: §1.15 gained the **transform-application rehearsal** bullet (parent `f89be0b`), the closed-artifact analogue of the git-fork target-resolution rehearsal — at generation, in a disposable scratch clone at `SOURCE_HEAD_SHA`, realize the plan-bound `updated_anchor` edits and run the slot's test/typecheck/artifact-evidence commands against the frozen target bundle (the artifact-evidence harness already selects every registry anchor, applies the full patch composition, and verifies sentinels without touching a live install — verified in `e2e/native-e2e.ts`); failures resolve by corrected re-derivation or fail-closed `removed_or_ambiguous_anchor`. Adversarially reviewed (independent repository-inspecting Opus reviewer: APPROVE, zero blocking; all four non-blocking findings folded in, including a source-truth correction — 2.1.200 generation informally ran only the drift scan, transforms were first exercised at execution baseline). **Escalation reason codes DONE (2026-07-05, unit advanced Track A — direction-doc Provisional Future Step 1)**: §1.17 is now a 20-code escalation registry (parent `5e80849`) — every run/cycle-ending STOP records exactly one code with a machine-checkable emission condition (source-§ cited), an invalidation scope in layer terms, and a resumption route; only `structural-*` codes enter level-2 re-derivation (environment/invoker-input/generation/executor classes resume in the routine path — the direction doc's containment requirement); the informal "a mass"/"most of the registry" structural triggers are bound to strict >50% majorities as fixed defaults; §1.18's confabulation class is an explicit non-code path (observer-attributed EXECUTOR-FAIL per §1.16); all five live-corpus STOP classes (runs 1–6) map. Adversarially reviewed (independent repository-inspecting Opus reviewer: REJECT — §2.7 double-classified; gate-11 genuine behavioral FAILs uncovered — both blocking findings plus three citation gaps fixed; re-review APPROVE, gate-11 partition verified machine-decidable via BLOCKED ≠ FAIL). **Direction-doc iteration 2 DONE (2026-07-05, unit advanced Track A)**: parent `81d85d2` supersedes the doc in place — Current State absorbs the §1.15 transform-application rehearsal and the §1.17 registry (four 2026-07-05 spec extensions recorded; escalation bullet now names the dispatcher as the remaining gap); **Next Step is the derivation pipeline** (prior Provisional Future Step 3) with an acceptance test binding its structural-break entry to the §1.17 `structural-*` codes and their invalidation scopes; the two owner-gated cycle-finishes stay listed as parallel non-blocking; renumbered future steps = dispatcher, contract-vs-bindings split, human-step reduction; End Goal untouched. Reviewed per the writing guidance: source-truth clean (Sonnet reviewer verified commit citations, the 20-code count, and the renumbering against git history and the spec), reader-state non-blocking findings fixed (host doc qualified at first §-citation; dangling previous-iteration references replaced with direct statements). **Derivation pipeline DONE (2026-07-05, unit advanced Track A — the direction doc's Next Step)**: specified as the sibling doc `docs/agent-specs/derivation-pipeline-spec.md` (parent `6a09f7c`; author's placement call, recorded there — the forward-port spec is the weak-tier executor's whole per-cycle input and is guarded by seal gate 12, so the owner-tier-only pipeline lives beside it, with a §1.17 pointer and an agent-specs README index line). Six staged, reviewer+judge-gated stages — capability discovery (fixed six-row probe list), integration-posture selection (decision keyed to the transcript-rewrite row; posture re-checkpoint at implementation contact), per-harness spec generation (the mined eleven-heading shared skeleton as output contract, contract-traceable vs exploration-derived provenance kept distinguishable), implementation (declared iteration budgets, persisted regression baseline, real-entry-point closure rule — all mined from the epic's failure record), e2e slot binding (new as a stage; binds the e2e spec's 8-slot contract, Protocol A once against a real build), level-2 binding emission (Part 4 slot table + command runbook; acceptance = a generated routine cycle plan passes §1.15). Entry conditions: all five §1.17 `structural-*` codes mapped to entry stage + untrusted-prior demotion set (§2.2 table); §11 traces both historical derivations stage-by-stage — the acceptance test's three prongs. Adversarially reviewed (independent repository-inspecting Opus reviewer + two fan-out corpus verifiers: APPROVE, zero blocking; all eight non-blocking findings folded in). **Standing steer (showrunner-relayed owner provenance, 2026-07-05, received mid-unit)**: doc-iteration yields are shrinking — bias next units toward *executable* progress (turn spec text into exercised behavior); further direction-doc iteration only when new run evidence arrives. **Path-selection dispatcher DONE (2026-07-05, unit advanced Track A — direction-doc Provisional Future Step 1, first unit under the executable-progress steer)**: parent `3f23054` adds `scripts/dispatch-escalation.mjs` + a 14-test suite (`node --test scripts/dispatch-escalation.test.mjs`, dependency-free Node) — reads an emitted §1.17 reason code (as an argument, or extracted from a report file) and routes it: routine-resume with the code's recorded resumption route, or level-2 re-derivation for `structural-*` codes with the §2.2 entry stage and demotion set. No copied registry: both spec tables are parsed at runtime, cross-checked (structural code sets AND invalidation scopes must agree), and any unknown code, ambiguous emission, malformed registry row, or spec drift fails closed at exit 2. The same commit binds the emission format in §1.17 ("Emission format and dispatch" paragraph): reports carry exactly one line `escalation-reason-code: <code>`; the dispatcher's output is a routing decision, not an authorization — owner gates still apply. Adversarially reviewed (independent repository-inspecting Opus reviewer: REJECT — the emission regex routed off mid-line prose mentions, a genuine fail-open — plus four hardening findings; all five fixed with tests; re-review APPROVE, reviewer re-ran probes and tests). The direction doc's "the dispatcher that consumes it is not built" line is now stale — superseded by `3f23054`, fold into the doc at its next evidence-driven iteration (do not iterate the doc for this alone, per the steer). **Automated release detection DONE (2026-07-05, unit advanced Track A — human-step reduction, direction-doc renumbered future step 3)**: parent `0cbbf93` adds `scripts/detect-pending-target.mjs` + a 14-test network-free suite (`node --test scripts/detect-pending-target.test.mjs`) and binds a machine-readable **Release detection** slot in Part 4 (§4.1 schema row; §4.2 OpenCode = highest `bonsai/v1-on-opencode-*` port tag vs `ls-remote upstream 'refs/tags/v*'`; §4.3 Claude Code = highest committed `semantic-anchor-analysis-<version>.md` vs `npm view`, package cross-checked against the Upstream identity slot). Like the dispatcher, the script parses the spec at runtime — no copied per-harness values; prereleases are excluded; any parse/query/cross-check anomaly (including ported-ahead-of-upstream) fails closed at exit 2; a newer stable upstream exits 3 with a `pending-target:` line. The signal is a detection, not an authorization — targets stay invoker-supplied, owner gates apply. Adversarially reviewed (independent repository-inspecting Opus reviewer: APPROVE, zero blocking; reviewer verified the spec claims against both repos' tag/doc history and probed the fail-open candidates live; one cosmetic finding folded in). **Live run at commit time: OpenCode up-to-date at 1.17.13; Claude Code emits a genuine pending-target 2.1.200 → 2.1.201 (exit 3) — the detector's first signal is real work.** **Maintenance-report fold DONE (2026-07-05, unit advanced Track A — step 1 of the 2.1.201 cycle start)**: parent `2d541ce` folds the sealed 2.1.200 cycle's deferred §1.16 spec-edit candidates into the forward-port spec — §3.5 relay-bookkeeping drift allowlist (side-repo `HAND_OFF.md` + `.agents/plans/**`, hardened with a plan/replay-set byte-identity re-check against the §1.14 item-11 approved commit); §3.6 live-scenario seeding discipline (exactly-one-match markers, reply-only-ok, no marker on the protected first message — the REAL-CYCLE marker-echo SPEC-GAP, placed in §3.6 rather than the report's §3.5 label, reviewer-judged correct); §4.3 platform-tarball frozen-binary route + `$TARGET_NATIVE_BINARY` explicit-path version assertions in preflight and live e2e (candidates a/b). Candidate (d) stays a flag, nothing invented. Adversarially reviewed (independent repository-inspecting Opus reviewer: APPROVE, zero blocking; four non-blocking findings folded in). The Phase-8 judgment call is resolved: its parked state adds no collision for 2.1.201 — artifact storage is versioned (`/tmp/cc-bonsai-artifacts/claude-code/2.1.201/` fresh), prior `/tmp/cc-cal-run-*` clones need only the §1.10 untouchable enumeration, parent dirt is the expected submodule pin, and the side repo's unpushed bookkeeping commits sit inside the frozen `SOURCE_HEAD_SHA` at generation time. Generation proceeds without waiting on Phase 8. Next unit = **Track A: generate the 2.1.201 cycle plan on the Fable tier through the full §1.15 loop** (step 2 of the cycle start) — freeze `@anthropic-ai/claude-code@2.1.201` per §3.1 via the §4.3 frozen-binary route (live install is 2.1.198 — do not touch it), run the anchor-drift scan against the frozen bundle, and note this is the first generation that must run §1.15's closed-artifact transform-application rehearsal and land a `semantic-anchor-analysis-2.1.201.md`; then (step 3) calibration-loop execution per "The loop" below (Opus-4.8-low executor, scratch clones). Run-5e evidence (worktree, commits `8e5b3b4` + 3 replay commits, records) preserved pending the Phase-8 owner decision. No pilot process runs. Full record: parent `.agents/pilot/gpt55-v1.17.13-observer-log.md`. `HANDOFF_GPT-5.5-opencode-pause.md` is historical.
- **History** (runs 1–4, commit ledger, scoping reconnaissance): `HAND_OFF_ARCHIVE_2026-07-03.md`, this directory.

## Standing operating rule: DRIVE (owner directive, 2026-07-05, verbatim intent)

**A parked chain is a failed chain.** Fable top-level agents exist to drive the project's high-level goals forward — that is the entire point. Do not present option menus and wait for Basil; decide, act, and give him an FYI with an override window. Basil called a prior park "complete negligence of duty." The only legitimate waits: outward actions (push/publish), truly destructive steps, or hard external blocks (e.g. provider quota with no fallback). Everything else — spec revisions, remediations, relaunches, tier decisions inside recorded intent — is yours to decide and execute. Report momentum, not options.

## What we are doing — two parallel tracks (owner directive, 2026-07-04)

The chain's work runs on two explicit tracks. Every hand-off notes which track its unit advanced; neither may displace the other.

**TRACK A — the self-maintaining meta-loop** (existing, primary): make Context Bonsai self-maintain across all its harness implementations via lower-level models — the spec, per-harness plugins, maintenance loop, and executor tiering. Everything in the paragraph below and "Immediate next action" is Track A unless marked otherwise.

**TRACK B — tune/hone the run-continuity mechanisms** (new, with a defined end state): the intent-log + reconciliation scaffolding introduced for pilot run 5e (parent `500e8a9`; design record in the parent's `.agents/pilot/gpt55-v1.17.13-observer-log.md`). Final goal: the mechanisms are **validated and generalized** — a weak executor (gpt-5.3-codex-spark tier) completes a full forward-port cycle with truthful, evidence-backed reports across continuation boundaries, and the honed mechanism is written up as reusable harness scaffolding (candidate for the meta-loop spec / per-harness plugins). Method: every run's failures feed concrete tuning changes to the mechanism, each attributed SPEC-GAP vs EXECUTOR-FAIL as usual. Track B is evidence for Track A's determinism-as-scaffolding tiering thesis, but it is its own goal, not a detour.

Making Context Bonsai self-maintain across all its harness implementations (the owner's aim, verbatim: "we are still aiming to make a process that will self-maintain Context Bonsai across all implementations"). Current focus is this repo's **Claude Code port**, ~44 npm releases behind (last cycle: `@anthropic-ai/claude-code@2.1.156`). The Fable relay chain — this chain of `claude-fable-5` sessions — authors and iterates the forward-port artifacts until an **Opus 4.8 subagent at low effort** (Agent tool: model `opus`, effort `low`) *reliably* executes the forward-port — repeated clean executions, not one lucky pass. Every stumble gets a verdict: `SPEC-GAP` (artifact under-specified — fix the artifact) vs `EXECUTOR-FAIL` (artifact deterministic, executor still failed — evidence about the tier). Observer/executor separation holds by prompt and artifact boundaries: never hint, fix, or pre-empt while an execution runs.

## Immediate next action

**PARKED for run-5 exit — the 2.1.200 forward-port cycle is SEALED.** The REAL-CYCLE executed 2026-07-03 by the calibrated Opus-4.8-low tier completed Phases 0–7 and 9 with zero STOPs; seal gates 1–13 pass; four commits `2c1c6fd`/`eef21e0`/`388830a`/`6fa187c` on this repo's `main`; no outward action taken (nothing pushed or published — the hard boundary held). Observer independently re-verified: live install untouched, frozen binary pristine, change-scope exactly planned ∪ allowlist, 165 tests + typecheck green at sealed HEAD, parent repo carrying only the expected submodule-pointer dirt with the forward-port spec untouched.

**Post-seal owner action (2026-07-03, verified):** Basil authorized directly in the watchdog session, which then pushed the side repo (`origin/main` = `4bd8747`) and advanced-and-pushed the parent submodule pin (parent commit `7c44a04`, pin → `4bd8747`). The push portion of landing thus happened early under explicit owner authorization, superseding the seal-time "nothing pushed" state below. Two consequences for the run-5 observer: the parent's pre-existing-dirt enumeration (constraint 1 and the plan's §1.10 record) **loses** its ` M tweakcc_context_bonsai` entry — parent tree is now clean; dirt was removed, not added, so gates that fail on unexpected NEW dirt are unaffected.

Nothing else is actionable until run 5 exits (self-resume ~2026-07-05 08:36 UTC; pause doc at top). Then, in order: (1) record the run-5 exit per its pause doc; (2) obtain the owner's Landing Authorization and record it in the plan's Approval section citing the run-5 exit-record parent commit; (3) execute the REMAINING Phase 8 scope — staged-artifact re-home (side-repo removal + parent copies), any stale-target-evidence parent-spec lines, final verification; note the pin will advance again when the staged copies are removed from the side repo, and Phase 8's own pin step handles that. Any further outward action stays owner-gated per the recorded boundary. The §1.16 maintenance report (committed, `.agents/plans/maintenance-report-f92dfac….md`) carries the Part 4 spec-edit candidates for after landing, including the one new REAL-CYCLE SPEC-GAP (E2E-08 seeding: the driven model echoes boundary markers, breaking exactly-one-match; recovered in-run) — fix it in the spec/next plan generation, not this sealed plan.

The loop (unchanged from run 1):

1. Provision a fresh scratch clone per run: `git clone /home/basil/projects/context-bonsai-agents/tweakcc_context_bonsai /tmp/cc-cal-run-N` (never point the executor at the real side repo; runs are serial — the `/tmp` artifacts are shared).
2. Spawn the executor at model `opus`, effort `low`. Mechanic note: the Agent tool in these sessions exposes no effort parameter — use the Workflow tool with a single `agent(prompt, { model: 'opus', effort: 'low' })` call. The launch prompt states `CALIBRATION` mode and the scratch root explicitly (the plan STOPs without it), names the plan path inside the clone, and instructs Phases 0–7 and 9 exactly as bound — nothing more (observer/executor separation).
3. Observe without contaminating (no hints, no fixes mid-run; ≤30-min wake-and-verify per intent rule 4).
4. After the run: verify the executor's claims against reality (live `claude --version` unchanged, frozen-binary sha256 pristine, real side repo untouched, evidence docs vs actual protocol/precedent), attribute every stumble `SPEC-GAP` vs `EXECUTOR-FAIL`, fix SPEC-GAPs in the plan on the Fable tier (amendments to `.agents/plans/` are relay-drift-allowlisted; run an independent repository-inspecting reviewer over the amendment diff before committing), and re-run. The bar is repeated clean executions, not one pass. Record run verdicts in State; park-and-slack only for genuine owner decisions the recorded intent does not cover.

## State

- 2026-07-03: HAND_OFF resealed (owner instruction; archive at `HAND_OFF_ARCHIVE_2026-07-03.md`). Cycle plan generated on the Fable tier, §1.15-validated (3 iterations, 7 blocking findings all fixed and none recurring, iteration-3 fixes mechanically verified; source-truth coverage review clean), staged and committed at `40d9233`. Anchor reality after 44 releases of drift: 7 of 8 anchors still select; `context-bonsai-gauge.token-usage` fails closed on a 52-vs-47 margin ambiguity — its semantic re-derivation is bound in the plan as the one expected `updated_anchor`.
- **Run 1** (2026-07-03, `/tmp/cc-cal-run-1`, ~28 min): Phases 0–5 clean (freeze verified, drift scan reproduced, token-usage re-derived semantically with thresholds untouched — scorer discriminators reviewed good; tests 164-pass green). 8 of 11 e2e rows genuine PASS incl. Protocol A and the prune-effect transform proof. STOPped correctly at seal gate 11. Verdicts: 2× EXECUTOR-FAIL (semantic-doc field header; effect-oracle seeded the protected first message — both self-corrected in-run under the retry rule); 2× SPEC-GAP (boundary-capture `jq` counted uuid-null metadata rows; E2E-00/04/05 had no bound verdict path — observer note: the executor drove the equally-unbound E2E-01/02/03/06 fine, so the gap was decidability, not possibility). Environment held: live install 2.1.198 untouched, frozen binary pristine, real repo clean. Run-1 evidence lives in `/tmp/cc-cal-run-1` (4 commits).
- **Amendment 1** committed `9d1458c` (independent Opus reviewer: APPROVE, zero blocking): E2E-00 bound to the local install-procedure predicate (2.1.156 executed precedent); E2E-05 bound to `PASS (unit-verified)` via two net-new Constraint-10 tests (run-1 review found the 2.1.156 "unit-verified" claim cited coverage that does not exist — real gap, closed); E2E-04 conditional reviewer+judge exception recorded (gauge live render has never been driven in any cycle; two compensating evidence conditions; Basil slacked 2026-07-03, `ok` — his override supersedes); `jq` string-uuid guard.
- **Run 2** (2026-07-03, `/tmp/cc-cal-run-2` at `9d1458c`, ~23 min): **first fully clean execution** — all phases green, zero STOPs, seal gates 1–13 pass, every Amendment-1 binding exercised as designed (E2E-00 `install-procedure-pass`, E2E-05 `compat-error-unit-verified` with the two net-new Constraint-10 tests, E2E-04 `PARTIAL` under the recorded exception with both compensating conditions evidenced). Observer re-verified: environment pristine, change-scope diff exactly planned ∪ allowlist, new tests re-run green. Two slips recurred identically from run 1 (still EXECUTOR-FAIL, self-corrected in-run) → judged removable.
- **Amendment 2** committed `2c40a24`: binds the semantic-doc format contract (the ten literal `validateSemanticReport` field labels + section-heading shape, mechanically verified against the validator source) and the E2E-08 seeding rule (no ALPHA on the protected first message). Mechanical decidability bindings only; no reviewer pass warranted (contrast Amendment 1, which touched scope/exception semantics and got one).
- **Run 3** (2026-07-03, `/tmp/cc-cal-run-3` at `2c40a24`, ~21 min, 133k tokens — costs trending down across runs): **clean AND slip-free**. Zero STOPs, seal gates pass, full e2e scope PASS/excepted, and neither recurring slip reproduced — both Amendment-2 bindings held on first attempt. Observer re-verified environment, scope, and the Constraint-10 tests independently. Residual looseness noted, not a stumble: Planned Target Files says anchors.test.ts is "extend[ed] for the re-derived token-usage evidence" but no bound Phase-4 step requires it — run 2 added a disambiguation test, run 3 did not; an Amendment-3 candidate if calibration continues.
- **Calibration closed 2026-07-03**: bar ruled MET (watchdog for the owner under standing delegation; owner override window open, delivery-verified; ghost-text disclaimer given with the ruling). Verdict ledger final: 2× SPEC-GAP fixed (Amendment 1), 2× recurring EXECUTOR-FAIL bound away (Amendment 2), zero unresolved. REAL-CYCLE authorized with the no-outward-action boundary; decision recorded in the plan validation history.
- **REAL-CYCLE** launched 2026-07-03 from `b751ace`, executor Opus-4.8-low: **SEALED** same day (~25 min, 180k tokens). Full e2e scope PASS with E2E-04 `PARTIAL` under the recorded exception. One in-run recovered stumble, flagged SPEC-GAP in the committed maintenance report (E2E-08 boundary-marker echo). The calibration thesis held: the artifact iterated on scratch clones executed unchanged against the real repo, first try, clean.
- **Owner-authorized early push** (2026-07-03, executed by the watchdog, verified by this chain): side repo → `origin/main` at `4bd8747`; parent pin advanced and pushed at `7c44a04`. Parent tree clean afterward. Remaining Phase 8 scope (artifact re-home, spec stale-lines, final verification) still gated on run-5 exit + Landing Authorization.

## Hard constraints and intent rules (from the owner, via direct conversation or the watchdog — Basil's top-layer supervising session; see Showrunner expectations at the end)

1. **Pilot-run discipline (updated 2026-07-04).** The run-5 quota pause is over: run 5 was killed under owner sanction, runs 5b/5c followed the same day (see the pilot pointer at top). While any pilot run lives: change nothing the executor reads, add no untracked files to the parent repo, avoid unsanctioned parent commits, observe under the ≤30-min wake-and-verify ceiling, and never interfere without watchdog sanction. No pilot run is currently live; run-5c leftovers are preserved evidence, owner-gated.
2. **Spec subtleties are load-bearing scar tissue.** The Claude Code spec's message-ordering rules (marker coverage of UUID-bearing system/meta rows, provider-side `api_system` filtering, retrieve-side marker removal) exist because of real post-prune ordering defects. Derived artifacts carry them explicitly and get a source-truth coverage review against the spec text. A subtlety that looks obsolete or in tension with the design is quoted to Basil via the watchdog — never deleted.
3. **Ghost text in claude input boxes is NOT user input.** The TUI renders AI-generated suggestions in idle input boxes; in a pane capture they are indistinguishable from typed-but-unsubmitted text and read like perfect answers. Never treat unsubmitted input-box text as Basil's instruction; confirm authorship first. Two owner-level decisions were wrongly executed from ghost text on 2026-07-03 before this rule existed.
4. **30-minute wait ceiling.** No agent below Basil's top layer may wait on anything — subagent, watcher, monitor, background process — longer than 30 minutes without waking and verifying actual state. Verify a watcher's signal actually reaches the watched channel before trusting it; silence is not evidence of progress.
5. **Re-read guidance docs, don't trust memory of them**: document authoring → the writing guidance (path at top); plans → `.llm-conductor/planning_guidance.md`; orchestration → `.llm-conductor/ORCHESTRATOR_AGENT.md`.
6. **Direct authoring over the formal plan system** for this work: Basil chose document authoring + the writing-guidance review loop; the forward-port spec's own §1.15 loop governs cycle plans.

## Fable credit economy (binding)

Fable tokens are scarce; spend them on judgment, synthesis, and intent-sensitive decisions only. Subagents inherit the session model — **always pass an explicit cheaper model** (`sonnet`, or `haiku` for mechanical work) when spawning readers, extractors, reviewers, or drafters. Finish a work unit, update this file, end the session; a long session reprocesses its whole history every turn. Read sources on demand; delegate bulk reading to cheap subagents and consume their conclusions.

## Relay protocol (tmux)

Sessions chain through tmux so each stays short. Your launch prompt names your predecessor's window; retiring it is your first act: `scripts/handoff-retire.sh <window-id>` — never raw `tmux kill-window` (the script only kills windows marked retiring and refuses self-targets). When your unit is done:

1. Update this file.
2. If blocked on input only Basil can give: do **not** relaunch. Slack him (`/home/basil/llm_prompts/scripts/slack.sh "..."`) and stop. Verify the script printed `ok` — anything else means Basil was NOT reached. In every blocked-on-Basil stop, end your final message with a line beginning `ATTENTION-BASIL:` stating in one sentence what you need — the watchdog session scans panes for that marker and answers on his behalf or relays.
3. Otherwise run `scripts/handoff-relay.sh` as your **final tool call** (output after it may be lost). It marks your window retiring and launches a fresh `claude --model claude-fable-5` pointed at this file.

Both scripts fail loudly outside tmux; if you are not in a tmux pane, skip the relay, update this file, and tell Basil the chain needs restarting from a tmux-launched session.

**Showrunner expectations**: `SHOWRUNNER_HANDOFF.md` in this directory is the top-layer watchdog session's own hand-off — deliberately gitignored, not your input, never committed. The watchdog may relay verified owner instructions into your session mid-turn; treat those as owner-provenance and record that provenance when acting on them.
